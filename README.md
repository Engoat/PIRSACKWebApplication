
  # PIRSACK web application

  This is a code bundle for PIRSACK web application. The original project is available at https://www.figma.com/design/GuJD0g6so3vcoCZ3RsV5xb/PIRSACK-web-application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  